Legacy compatibility source area for trainer migration.

Canonical trainer modules are now:

- `:trainer:gollek-trainer-api` at `trainer/gollek-trainer-api`
- `:trainer:gollek-trainer` at `trainer/gollek-trainer`

Do not add new Gradle runtime code here.
Use `trainer/gollek-trainer` for canonical trainer runtime changes.
