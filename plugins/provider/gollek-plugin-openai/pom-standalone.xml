<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- Standalone plugin - no parent POM required -->
    <groupId>tech.kayys.gollek</groupId>
    <artifactId>gollek-plugin-openai</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>Gollek Plugin :: OpenAI Cloud Provider</name>
    <description>OpenAI cloud provider plugin for Gollek inference engine</description>
    <url>https://github.com/your-org/gollek-plugin-openai</url>

    <properties>
        <!-- Java version -->
        <maven.compiler.release>25</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <!-- Gollek version - update this for your target Gollek version -->
        <gollek.version>1.0.0-SNAPSHOT</gollek.version>

        <!-- Dependency versions -->
        <mutiny.version>2.9.4</mutiny.version>
        <jackson.version>2.18.3</jackson.version>
        <jboss-logging.version>3.6.1.Final</jboss-logging.version>

        <!-- Plugin versions -->
        <maven-compiler-plugin.version>3.14.0</maven-compiler-plugin.version>
        <maven-jar-plugin.version>3.4.2</maven-jar-plugin.version>
        <maven-shade-plugin.version>3.6.0</maven-shade-plugin.version>
        <maven-deploy-plugin.version>3.1.4</maven-deploy-plugin.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Optional: Import Gollek BOM if available -->
            <!--
            <dependency>
                <groupId>tech.kayys.gollek</groupId>
                <artifactId>gollek-bom</artifactId>
                <version>${gollek.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            -->
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- ================================================ -->
        <!-- Gollek SPI Dependencies (provided by runtime)    -->
        <!-- ================================================ -->

        <!-- Provider SPI - Core provider interface -->
        <dependency>
            <groupId>tech.kayys.gollek</groupId>
            <artifactId>gollek-spi-provider</artifactId>
            <version>${gollek.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- Inference SPI - Inference types -->
        <dependency>
            <groupId>tech.kayys.gollek</groupId>
            <artifactId>gollek-spi-inference</artifactId>
            <version>${gollek.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- Core SPI - Common types -->
        <dependency>
            <groupId>tech.kayys.gollek</groupId>
            <artifactId>gollek-spi</artifactId>
            <version>${gollek.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- ================================================ -->
        <!-- Reactive & Logging (provided by runtime)         -->
        <!-- ================================================ -->

        <!-- Mutiny for reactive types -->
        <dependency>
            <groupId>io.smallrye.reactive</groupId>
            <artifactId>mutiny</artifactId>
            <version>${mutiny.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- Logging -->
        <dependency>
            <groupId>org.jboss.logging</groupId>
            <artifactId>jboss-logging</artifactId>
            <version>${jboss-logging.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- ================================================ -->
        <!-- Jackson for JSON (provided by runtime)           -->
        <!-- ================================================ -->

        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- ================================================ -->
        <!-- Testing Dependencies                             -->
        <!-- ================================================ -->

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>5.11.4</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <version>5.15.2</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- ================================================ -->
            <!-- Compiler Configuration                           -->
            <!-- ================================================ -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven-compiler-plugin.version}</version>
                <configuration>
                    <release>${maven.compiler.release}</release>
                    <parameters>true</parameters>
                </configuration>
            </plugin>

            <!-- ================================================ -->
            <!-- JAR Plugin with Plugin Manifest                  -->
            <!-- ================================================ -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>${maven-jar-plugin.version}</version>
                <configuration>
                    <archive>
                        <manifest>
                            <addDefaultImplementationEntries>true</addDefaultImplementationEntries>
                            <addDefaultSpecificationEntries>true</addDefaultSpecificationEntries>
                        </manifest>
                        <!-- Plugin metadata for hot-loading -->
                        <manifestEntries>
                            <Plugin-Id>openai-cloud-provider</Plugin-Id>
                            <Plugin-Version>${project.version}</Plugin-Version>
                            <Plugin-Class>tech.kayys.gollek.plugin.cloud.openai.OpenAiCloudProvider</Plugin-Class>
                            <Plugin-Provider>tech.kayys.gollek</Plugin-Provider>
                            <Plugin-Description>OpenAI cloud provider for GPT-4, GPT-3.5-turbo
                                models</Plugin-Description>
                        </manifestEntries>
                    </archive>
                </configuration>
            </plugin>

            <!-- ================================================ -->
            <!-- Deploy Plugin (optional - for publishing)        -->
            <!-- ================================================ -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-deploy-plugin</artifactId>
                <version>${maven-deploy-plugin.version}</version>
                <configuration>
                    <skip>false</skip>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!-- ================================================ -->
        <!-- Profile: Fat JAR with all dependencies           -->
        <!-- Use for standalone deployment                    -->
        <!-- ================================================ -->
        <profile>
            <id>fat-jar</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-shade-plugin</artifactId>
                        <version>${maven-shade-plugin.version}</version>
                        <executions>
                            <execution>
                                <phase>package</phase>
                                <goals>
                                    <goal>shade</goal>
                                </goals>
                                <configuration>
                                    <shadedArtifactAttached>true</shadedArtifactAttached>
                                    <shadedClassifierName>all</shadedClassifierName>
                                    <filters>
                                        <filter>
                                            <artifact>*:*</artifact>
                                            <excludes>
                                                <exclude>META-INF/*.SF</exclude>
                                                <exclude>META-INF/*.DSA</exclude>
                                                <exclude>META-INF/*.RSA</exclude>
                                            </excludes>
                                        </filter>
                                    </filters>
                                    <transformers>
                                        <transformer
                                            implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer" />
                                        <transformer
                                            implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                            <manifestEntries>
                                                <Plugin-Id>openai-cloud-provider</Plugin-Id>
                                                <Plugin-Version>${project.version}</Plugin-Version>
                                                <Plugin-Class>
                                                    tech.kayys.gollek.plugin.cloud.openai.OpenAiCloudProvider</Plugin-Class>
                                            </manifestEntries>
                                        </transformer>
                                    </transformers>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!-- ================================================ -->
        <!-- Profile: Copy to Gollek Plugins Directory        -->
        <!-- Use for local development                        -->
        <!-- ================================================ -->
        <profile>
            <id>install-plugin</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-antrun-plugin</artifactId>
                        <version>3.1.0</version>
                        <executions>
                            <execution>
                                <id>copy-to-gollek-plugins</id>
                                <phase>install</phase>
                                <goals>
                                    <goal>run</goal>
                                </goals>
                                <configuration>
                                    <target>
                                        <mkdir dir="${user.home}/.gollek/plugins" />
                                        <copy
                                            file="${project.build.directory}/${project.build.finalName}.jar"
                                            todir="${user.home}/.gollek/plugins" />
                                        <echo
                                            message="Plugin installed to ${user.home}/.gollek/plugins/" />
                                    </target>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!-- ================================================ -->
        <!-- Profile: Release (sign artifacts, deploy)        -->
        <!-- ================================================ -->
        <profile>
            <id>release</id>
            <build>
                <plugins>
                    <!-- Source JAR -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-source-plugin</artifactId>
                        <version>3.3.1</version>
                        <executions>
                            <execution>
                                <id>attach-sources</id>
                                <goals>
                                    <goal>jar-no-fork</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>

                    <!-- Javadoc JAR -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-javadoc-plugin</artifactId>
                        <version>3.11.2</version>
                        <executions>
                            <execution>
                                <id>attach-javadocs</id>
                                <goals>
                                    <goal>jar</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>

                    <!-- GPG Sign (optional) -->
                    <!--
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <version>3.2.7</version>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>sign</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    -->
                </plugins>
            </build>
        </profile>
    </profiles>

    <!-- ================================================ -->
    <!-- Distribution Management (optional)               -->
    <!-- ================================================ -->
    <distributionManagement>
        <repository>
            <id>central</id>
            <name>Central Repository</name>
            <url>https://repo.maven.apache.org/maven2</url>
        </repository>
        <!-- Or your own Nexus/Artifactory -->
        <!--
        <repository>
            <id>nexus-releases</id>
            <name>Nexus Release Repository</name>
            <url>https://your-nexus.com/repository/maven-releases/</url>
        </repository>
        <snapshotRepository>
            <id>nexus-snapshots</id>
            <name>Nexus Snapshot Repository</name>
            <url>https://your-nexus.com/repository/maven-snapshots/</url>
        </snapshotRepository>
        -->
    </distributionManagement>

    <!-- ================================================ -->
    <!-- SCM (optional)                                   -->
    <!-- ================================================ -->
    <scm>
        <connection>scm:git:git://github.com/your-org/gollek-plugin-openai.git</connection>
        <developerConnection>scm:git:ssh://github.com/your-org/gollek-plugin-openai.git</developerConnection>
        <url>https://github.com/your-org/gollek-plugin-openai</url>
        <tag>HEAD</tag>
    </scm>

    <!-- ================================================ -->
    <!-- Licenses                                         -->
    <!-- ================================================ -->
    <licenses>
        <license>
            <name>MIT License</name>
            <url>https://opensource.org/licenses/MIT</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <!-- ================================================ -->
    <!-- Developers                                       -->
    <!-- ================================================ -->
    <developers>
        <developer>
            <id>your-id</id>
            <name>Your Name</name>
            <email>your.email@example.com</email>
            <organization>Your Organization</organization>
        </developer>
    </developers>
</project>