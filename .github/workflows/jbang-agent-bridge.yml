name: JBang Agent Bridge

on:
  pull_request:
    paths:
      - "build.gradle.kts"
      - "gradle/jbang-trainer-examples.gradle.kts"
      - "examples/jbang/integration/wayang_gollek_serving_bridge.java"
      - "examples/jbang/INDEX.md"
      - "sdk/gollek-sdk-agent/**"
      - "sdk/gollek-sdk-remote/**"
      - ".github/workflows/jbang-agent-bridge.yml"
  push:
    branches:
      - main
      - master
    paths:
      - "build.gradle.kts"
      - "gradle/jbang-trainer-examples.gradle.kts"
      - "examples/jbang/integration/wayang_gollek_serving_bridge.java"
      - "examples/jbang/INDEX.md"
      - "sdk/gollek-sdk-agent/**"
      - "sdk/gollek-sdk-remote/**"
      - ".github/workflows/jbang-agent-bridge.yml"
  workflow_dispatch:

jobs:
  bridge-smoke:
    name: Wayang-Gollek Bridge Smoke
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "25"
          cache: gradle

      - name: Setup JBang
        shell: bash
        run: |
          set -euo pipefail
          curl -Ls https://sh.jbang.dev | bash -s - app setup
          echo "$HOME/.jbang/bin" >> "$GITHUB_PATH"

      - name: Smoke Wayang-Gollek bridge
        shell: bash
        run: |
          set -euo pipefail
          ./gradlew --no-daemon smokeJbangAgentBridgeExamples \
            -Pgollek.jbang.agentBridge.offline=false
